using System;
using System.Collections.Generic;
using System.Linq;
//using System.Reflection.Metadata.Ecma335;
using System.Text;
using System.Threading.Tasks;

namespace CarsOOPGruppProjekt
{
    internal class ShowCars
    {
       /* private List<Cars> _carsList;

        public ShowCars()
        {
            refreshCarsList();
        }

        public void refreshCarsList()
        {
            sqlConnection sqlCon = new sqlConnection();
            _carsList = sqlCon.getData();
        }

        public void refreshSQLCars(ListBox ShowCListBox)
        {
            foreach (Cars superCar in _carsList)
            {
                string data = ("Model: " + superCar.cars_model + " Price: " + superCar.cars_price.ToString() + " RetailerName: " + superCar.retailers_retailers_name + " ");
                ShowCListBox.Items.Add(data);
            }
        }

        public List<Cars> CarsList
        {
            get { return _carsList; }
            set { _carsList = value; }
        }*/
    }
}
